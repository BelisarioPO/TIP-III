import React from "react";
function Result({results}) {
    return(
        // <ul>
        //   {results.map((result) => (
        //     <li key={result.id}>{result.title}</li>
        //   ))}
        // </ul>
        <h1>Result</h1>
        
        
        )
}
export default Result;